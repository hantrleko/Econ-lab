---
description: 核心证据纪律。禁止虚构文献与结果；claim 必须能指回本地证据。
alwaysApply: true
---

# 核心纪律

- 禁止虚构文献、DOI、期刊页码、数据包链接、回归系数或标准误。
- 每条事实性 claim 必须能指回本地 `tables/`、`figures/`、代码、日志或 `papers/` 卡片。指不回去就标 `UNVERIFIED` 或删掉。
- 没打开 PDF、没跑通代码、没核开 DOI：写「未核验」，不要补全「看起来合理」的细节。
- 五层 credibility 不可混谈：能复现 ≠ 实现了对的估计量 ≠ 有限样本表现好 ≠ 变量测到构念 ≠ 识别成立。汇报检查时写明层级。
- 已投稿旧稿不是默认任务。用户未点名，不要去改那些文章。
- 不确定时停笔，问用户或标 `UNVERIFIED`。不要用流畅句子掩盖缺口。
