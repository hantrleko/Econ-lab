# Replication README

- 项目：
- 作者：
- 最后跑通日期：
- 软件：Stata __ / Python __

不要填写未核验的数据 URL。数据若需申请，写申请处与许可，不写假链接。

## 仓库地图

| 路径 | 内容 |
|---|---|
| `data/raw/` | 只读原始文件（不提交） |
| `data/clean/` | 由 `code/` 生成 |
| `code/stata/` | |
| `code/python/` | |
| `tables/` | |
| `figures/` | |

## 环境

- Stata 版本 / 需要的社区包（`ssc install` 列表）：
- Python 版本 / `requirements`（若有）：
- 随机种子：

## 运行顺序

1. 把 raw 放到 `data/raw/`（来源说明：）
2. 运行 `code/...`
3. 期望写出的表图：

## 样本与键

- 观察单元：
- 时间：
- 处理层级与聚类层级：

## 已知限制

哪些结果只在特定样本成立；哪些脚本尚未跑通（UNVERIFIED）。
