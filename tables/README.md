# tables/

脚本写出的表，不是手敲的终稿。

## 命名

```
T<number>_<purpose>.tex
```

例：`T1_main.tex`、`T2_robust_bw.tex`、`T3_placebo.tex`

同一估计的 csv / rtf 副本可用相同词干。

## 每张表在卡片或 memo 里应能回答

- 样本与时间
- estimand
- 标准误聚类
- 由哪条脚本、哪份 clean 数据生成

正文引用表号必须与文件名一致。对不上就改文或改名，不要含糊跳过。
